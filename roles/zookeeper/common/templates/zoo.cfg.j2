# Quorum
{% for zk_server in zk_servers %}
server.{{ loop.index }}={{ zk_server }}
{% endfor %}

# Cluster properties
{% for name, value in (zookeeper_cfg | dictsort) %}
{{ name }}={{ value }}
{% endfor %}
