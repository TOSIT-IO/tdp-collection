---
- import_role:
    name: tosit.tdp.utils.kerberos
    tasks_from: install

- import_role:
    name: tosit.tdp.utils.kerberos
    tasks_from: create_headless_principal_keytab
  vars:
    service_principal: "yarn"
    service_keytab: "yarn.headless.keytab"
    service_user: "{{ yarn_user }}"
    service_group: "{{ hadoop_group }}"
    service_mode: "600"
