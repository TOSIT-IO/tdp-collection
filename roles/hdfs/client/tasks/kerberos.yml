---
- import_role:
    name: tosit.tdp.utils.kerberos
    tasks_from: install

- import_role:
    name: tosit.tdp.utils.kerberos
    tasks_from: create_headless_principal_keytab
  vars:
    principal: "hdfs"
    keytab: "hdfs.headless.keytab"
    user: "{{ hdfs_user }}"
    group: "{{ hadoop_group }}"
    mode: "600"
