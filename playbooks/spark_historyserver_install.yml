---
- name: Install Spark History Server
  hosts: spark_hs
  tasks:
    - tosit.tdp.resolve:
        node_name: spark_historyserver
    - import_role:
        name: tosit.tdp.spark.historyserver
        tasks_from: install
    - meta: clear_facts
